[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build -- --configuration production"]

[start]
cmd = "npx serve -s dist/tienda-pachito-frontend/browser -l $PORT"